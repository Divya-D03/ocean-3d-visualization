# API routers package
# TODO (Backend API team): add endpoint routers here and include them in app/main.py
# Example:
#   from fastapi import APIRouter
#   router = APIRouter(prefix="/observations", tags=["observations"])
